from modelo.copiapp_modelo import ModeloPunto


class ControladorPuntos:

    def __init__(self)
    self.modelo = ModeloPunto()

    def getPuntos(self):
        tabla = 'sedes'
        res = self.modelo.mostrarSedes(tabla)
        return tabla
